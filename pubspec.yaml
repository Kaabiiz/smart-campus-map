name: smart_campus_map
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8

  # Maps & Location
  flutter_map: ^8.2.2
  latlong2: ^0.9.1

  http: ^1.2.0 

  # Email Service
  #emailjs: ^4.0.0

  # ✅ ADD THESE NEW DEPENDENCIES:
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.3
  intl: ^0.19.0  # For date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true